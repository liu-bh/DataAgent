apiVersion: apps/v1
kind: Deployment
metadata:
  name: agent-service
  namespace: datapilot
  labels:
    app: agent-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: agent-service
  template:
    metadata:
      labels:
        app: agent-service
    spec:
      containers:
        - name: agent-service
          image: ghcr.io/liu-bh/dataagent/agent-service:latest
          ports:
            - containerPort: 8000
          env:
            - name: AGENT_PORT
              value: "8000"
            - name: LOG_LEVEL
              valueFrom:
                configMapKeyRef:
                  name: datapilot-config
                  key: LOG_LEVEL
          resources:
            requests:
              memory: "256Mi"
              cpu: "100m"
            limits:
              memory: "512Mi"
              cpu: "500m"
          readinessProbe:
            httpGet:
              path: /health
              port: 8000
            initialDelaySeconds: 10
            periodSeconds: 15
          livenessProbe:
            httpGet:
              path: /health
              port: 8000
            initialDelaySeconds: 20
            periodSeconds: 30
---
apiVersion: v1
kind: Service
metadata:
  name: agent-service
  namespace: datapilot
spec:
  selector:
    app: agent-service
  ports:
    - port: 8000
      targetPort: 8000
  type: ClusterIP
