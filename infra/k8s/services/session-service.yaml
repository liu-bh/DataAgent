apiVersion: apps/v1
kind: Deployment
metadata:
  name: session-service
  namespace: datapilot
  labels:
    app: session-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: session-service
  template:
    metadata:
      labels:
        app: session-service
    spec:
      containers:
        - name: session-service
          image: ghcr.io/liu-bh/dataagent/session-service:latest
          ports:
            - containerPort: 8006
          env:
            - name: SESSION_PORT
              value: "8006"
            - name: LOG_LEVEL
              valueFrom:
                configMapKeyRef:
                  name: datapilot-config
                  key: LOG_LEVEL
          resources:
            requests:
              memory: "256Mi"
              cpu: "100m"
            limits:
              memory: "512Mi"
              cpu: "500m"
          readinessProbe:
            httpGet:
              path: /health
              port: 8006
            initialDelaySeconds: 10
            periodSeconds: 15
          livenessProbe:
            httpGet:
              path: /health
              port: 8006
            initialDelaySeconds: 20
            periodSeconds: 30
---
apiVersion: v1
kind: Service
metadata:
  name: session-service
  namespace: datapilot
spec:
  selector:
    app: session-service
  ports:
    - port: 8006
      targetPort: 8006
  type: ClusterIP
