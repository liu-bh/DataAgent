apiVersion: apps/v1
kind: Deployment
metadata:
  name: auth-service
  namespace: datapilot
  labels:
    app: auth-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: auth-service
  template:
    metadata:
      labels:
        app: auth-service
    spec:
      containers:
        - name: auth-service
          image: ghcr.io/liu-bh/dataagent/auth-service:latest
          ports:
            - containerPort: 8004
          env:
            - name: AUTH_PORT
              value: "8004"
            - name: LOG_LEVEL
              valueFrom:
                configMapKeyRef:
                  name: datapilot-config
                  key: LOG_LEVEL
          resources:
            requests:
              memory: "256Mi"
              cpu: "100m"
            limits:
              memory: "512Mi"
              cpu: "500m"
          readinessProbe:
            httpGet:
              path: /health
              port: 8004
            initialDelaySeconds: 10
            periodSeconds: 15
          livenessProbe:
            httpGet:
              path: /health
              port: 8004
            initialDelaySeconds: 20
            periodSeconds: 30
---
apiVersion: v1
kind: Service
metadata:
  name: auth-service
  namespace: datapilot
spec:
  selector:
    app: auth-service
  ports:
    - port: 8004
      targetPort: 8004
  type: ClusterIP
