[project]
name = "datapilot-semantic"
version = "0.1.0"
description = "DataPilot Semantic Service: models, metadata, vector search"
requires-python = ">=3.11"
dependencies = [
    "fastapi>=0.115",
    "uvicorn[standard]>=0.32",
    "datapilot-common",
]
