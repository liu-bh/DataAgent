[project]
name = "datapilot-sqlgen"
version = "0.1.0"
description = "DataPilot SQL Generator Service: NL2SQL core, AST builder, validation"
requires-python = ">=3.11"
dependencies = [
    "fastapi>=0.115",
    "uvicorn[standard]>=0.32",
    "datapilot-common",
    "datapilot-sql",
]
